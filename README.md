# clone-tabnews
clone do projeto tabnews.com.br (curso.dev)

> ## Dia 3
- to define the project default nvm version
    - create the file .nvmrc
-  to define node version
    - nvm alias default lts/hydrogen
- install (using the alias default)
    - nvm install
    